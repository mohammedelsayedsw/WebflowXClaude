# 10 — Architecture

<!-- Routing, file conventions, where pages live. -->

## File conventions

<!-- TODO: fill in. Example below. -->

- Pages live in `src/app/<route>/page.tsx`.
- Webflow components (legacy, allowlisted) live in `src/webflow/`.
- Local custom components live in `src/components/`.
- Local custom sections live in `src/sections/`.

## Routing

<!-- TODO -->
