# 40 — Rules

<!-- Hard rules: what NOT to do. -->

<!-- TODO: fill in. Example below. -->

- Do not invent components; only use entries from `20-components.md` / `21-sections.md`.
- New reusable blocks go in `src/components/` or `src/sections/`, never inline in a page.
- Every page follows the structure in `15-page-template.md`.
